import React from 'react';
import { Card } from 'antd';

const AIRecommend: React.FC = () => {
  return (
    <Card title="AIRecommend">
      <p>功能开发中...</p>
    </Card>
  );
};

export default AIRecommend;
